# KAIROX Intelligence — 議事録
**日時:** 2026年3月27日
**参加者:** Tsune（CEO）、Engineer、Secretary
**議長:** Tsune
**書記:** Secretary Agent

---

## 1. 独自ドメイン取得・設定完了（task:7）

| 項目 | 内容 |
|------|------|
| ドメイン | `kairox.jp` |
| 登録日 | 2026-03-27（お名前.com） |
| 登録期限 | 2027-03-31（自動更新設定済み） |
| DNS管理 | お名前.com → Vercel DNS に移管（ns1/ns2.vercel-dns.com） |
| Vercel追加 | `kairox.jp` / `www.kairox.jp` ✅ |
| SSL証明書 | Vercel自動発行 ✅ |
| 動作確認 | `https://kairox.jp/narita` ✅ |

### 旧ドメイン整理

| ドメイン | 対応 |
|---|---|
| `kairox.1hourvalue.com` | Vercel削除・DNSレコード削除 ✅ |
| `1hourvalue.online` | 自動更新オフ（2027-03-27期限、自然失効予定） |

---

## 2. 旧URL リダイレクト（middleware.ts）

- `*.vercel.app` 等の旧URLへのアクセスを `kairox.jp` に 301リダイレクト
- `/driver` `/admin` も同様に新ドメインへ統一
- `frontend/middleware.ts` 新規作成・本番デプロイ済み

---

## 3. Resend メール設定完了

| 項目 | 状態 |
|---|---|
| Resendアカウント | 既存アカウント使用 |
| ドメイン追加 | `kairox.jp` ✅ |
| DKIM（TXT） | `resend._domainkey` → Vercel DNS ✅ |
| SPF MX | `send` → `feedback-smtp.ap-northeast-1.amazonses.com` ✅ |
| SPF TXT | `send` → `v=spf1 include:amazonses.com ~all` ✅ |
| DMARC（TXT） | `_dmarc` → `v=DMARC1; p=none;` ✅ |
| APIキー | Vercel・Railway に設定済み ✅ |
| ドメイン認証 | DNS反映待ち（最大72時間） |

> ⚠️ APIキーがチャットに露出したため、本番運用前に再生成が必要

---

## 4. PostgreSQL 移行完了

- 従来: SQLite（Railwayファイルシステム・再デプロイで消去）
- 現在: Railway PostgreSQL に移行
- `DATABASE_URL` を Railway サービス変数に設定（`${{PostgreSQL.DATABASE_URL}}`）
- テーブル自動作成・バックエンド v0.11.0 稼働確認済み
- 旧SQLiteデータ（テスト予約）は消去済み

---

## 5. コード追加（TrackingView + api/review）

| コンポーネント | 機能 |
|---|---|
| `PlayerProgressBar` | ハンドキャリー路線進捗バー（空港→電車→徒歩→完了）|
| `NearbyPlayersPanel` | GPS近隣プレイヤー検索 → dispatchリクエスト |
| `ReviewForm` | 配達完了後レビュー（★評価・時間通り・コメント・4言語）|
| `app/api/review/route.ts` | `/api/review` → Railway プロキシルート |

---

## 6. インフラ状態（2026-03-27 最終）

| コンポーネント | 状態 | URL |
|---|---|---|
| 本番ドメイン | ✅ 稼働中 | https://kairox.jp/narita |
| Railway バックエンド | ✅ 稼働中 v0.11.0 | https://kairox-production.up.railway.app |
| PostgreSQL | ✅ 接続済み | Railway内部 |
| Company AI | ✅ 稼働中 | /api/company |
| AviationStack | ✅ 実データ接続 | /api/flights |
| Google Directions | ✅ 実データ接続 | /api/eta |
| Resend | ⚠️ DNS認証待ち（72h以内） | — |
| Stripe | ⚠️ テストキーのみ | — |

---

## 7. 残タスク

| 優先度 | タスク | 備考 |
|---|---|---|
| ⚠️ 急 | Resend APIキー再生成 | チャット露出のため |
| ⚠️ 急 | Resend ドメイン認証確認 | DNS反映後 |
| P0 | ドライバー/プレイヤー確保・黒ナンバー | in_progress |
| P0 | `info@kairox.jp` 送信テスト | Resend認証後 |
| P1 | Stripe 本番キー | CEO申請待ち |
| P1 | 営業時間制御 本番有効化 | ドライバー確保後 |
| P2 | 羽田フロー `/haneda` | ドライバー確保後 |
| P3 | 信頼スコア自動計算 | 未着手 |
| P3 | Stripe Connect 報酬送金 | Stripe本番後 |
