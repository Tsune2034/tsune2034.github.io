# KAIROX リリースノート

---

## v0.13.0 — 2026-04-01

### GPS地図全面刷新・双方向チャット・ドライブモード（15コミット）

**主な変更点:**
- GPS地図 マーカー非表示レースコンディション修正（applyPoints共通化）
- 成田空港ピン常時表示・Googleマップナビボタン追加
- ヘディングアップ（進行方向UP）・ドライブモード（地図560px・ミラーリング対応）
- 顧客→ドライバー 4種プリセットチャット（TrackingView）
- ドライバー→顧客 3種プリセットチャット（DriverView）
- 30秒双方向ポーリング・NEWバッジ（5分以内アンバー点滅）
- 税関ボタン廃止 → 着陸後45分カウントダウンで代替
- GPS ノイズ除去（15m閾値）・100点スライディングウィンドウ
- 成田500m以内で「近くにいます」自動ステータス切替
- DB: customer_message / driver_message / customs_exited カラム追加（idempotent）
- 成田テスト走行 GPS学習データ初記録（16サンプル・23セグメント）

→ 詳細: `docs/releases/リリースノート_v0.13.0.md`

---

## v0.7.0 — 2026-03-20

### Stripe決済統合・予約確認メール・バグ修正

**新機能: Stripe CardElement 統合**
- `@stripe/react-stripe-js` 導入。手動カード入力フィールドを廃止しセキュアな CardElement に置換
- `page.tsx` を `<Elements>` でラップ。Dashboard 内で `useStripe` / `useElements` を使用
- 支払いフロー：PaymentIntent 作成（`POST /payments/create-intent`）→ `stripe.confirmCardPayment()` → 予約作成
- バックエンド `POST /payments/create-intent` エンドポイント追加（`STRIPE_SECRET_KEY` 未設定時はモックレスポンス）
- `frontend/app/api/payment/route.ts` プロキシルート追加
- 「🔒 Powered by Stripe — PCI DSS準拠」表記追加

**新機能: クレジットカード手数料 10% 表示**
- `CARD_SURCHARGE_RATE = 0.10` 定数追加
- 支払い方法タブにバッジ：クレジット `+10%`（赤）/ JPYC・USDC `0%`（緑）
- 料金内訳に「💳 カード決済手数料」行を追加。JPYC/USDC との差別化を可視化
- `calcTotal()` がカード選択時のみ手数料を加算

**新機能: 予約確認メール（Resend）**
- `backend/email.py` 追加：HTML メールテンプレート + `send_booking_confirmation()`
- 予約作成後に自動送信（失敗しても予約は成立・KRX番号は発行される）
- メール内容：追跡番号（大）・予約詳細テーブル・追跡リンクボタン・キャンセルポリシー
- `RESEND_API_KEY` 未設定または email 空の場合は無音でスキップ
- 送信元：`onboarding@resend.dev`（独自ドメイン認証後に `noreply@kairox.jp` へ変更予定）
- `RESEND_FROM` 環境変数でオーバーライド可能

**バグ修正**
- 配達完了がPC側（顧客追跡画面）に反映されない問題を修正
  - バックエンド：`done → delivered`・`nearby → transit` のステータスマッピングを追加
  - `DriverLocationUpdate` モデルの `lat` / `lng` を `Optional[float]` に変更
- GPS OFF 時にステータス変更がバックエンドに送信されない問題を修正
  - `pushStatus()` 関数を新設。GPS ON/OFF に関わらずステータス変更は必ずバックエンドに送信
  - GPS 取得失敗時も `pushStatus()` にフォールバック
- `DriverView` の画面上 PIN 表示を削除（セキュリティ対応）

**ドキュメント**
- `public/sop-v1.html` — システム動作確認書・標準操作手順書（SOP v1.0）追加
  - 旅行者 / ドライバー / 追跡フロー の各 SOP（ステップ形式）
  - 動作確認チェックリスト 14 項目（□ 確認欄付き）
  - パートナー向け説明トーク例・トラブルシューティング表

---

## v0.6.0 — 2026-03-20

### 富良野ゾーン・ドライバー登録ページ・GPS近接割引テロップ

**新機能: 富良野・美瑛ゾーン追加**
- Zone 型に `furano` を追加（旭川空港 AKJ 経由）
- 料金：Solo ¥5,500 / Pair ¥9,000 / Family ¥15,000
- 集荷スポット 4 件：旭川空港到着ロビー / 旭川駅 / 富良野駅 / ホテルフロント前
- ホテルリスト 12 件（新富良野プリンスホテル・OMO7旭川・ホテル日航旭川等）
- ゾーンセレクターを 2 列グリッドに変更（4 ゾーン対応）
- i18n：全 4 言語対応（"富良野・美瑛（旭川空港経由）"）
- 旭川空港→富良野エリアは競合空白地帯（LuggAgent は CTS コリドーに集中）

**新機能: 新千歳空港配送の有効化**
- `new_chitose` を Destination として有効化
- 配送先スポット 6 件（国際線チェックイン前 / 無印良品前 / スタバ前 / ANA・JAL カウンター前 / 到着ロビー）
- 成田・羽田は disabled ボタン + 「近日公開」バッジで表示維持

**新機能: GPS近接ドライバー割引テロップ（DriverNearbyTicker）**
- `ZONE_CENTERS` 配列でサービスゾーン 4 地点を定義
- 顧客が 30km 圏内 + 営業時間内（6〜22 時）のとき予約画面上部に自動表示
- `globals.css` に `@keyframes marquee` を追加。スクロールテロップ表示
- LIVE パルスアニメーションバッジ付き・4 言語対応
- 支払い画面に「🚐 近接ドライバー即時割引 −¥300」として内訳表示
- ドライバーの空走を有償化する需要平準化メカニズム

**新機能: BusinessView ドライバー登録タブ**
- `biz_tab_corporate` / `biz_tab_driver` タブスイッチャー追加
- `DriverSection`：収入シミュレーション（KAIROX ¥270,000 vs 業界大手 ¥180,000）
- 還元率比較バー（KAIROX 85〜90% vs 大手 60〜70%）
- ドライバー登録フォーム（名前・電話・車両種別・エリア・稼働スタイル）
- i18n：`drv_*` キー 30 項目以上を全 4 言語追加

**その他**
- 小樽 OMO5（星野リゾート）をホテルリストに追加
- `TrackingView` に mock DriverCard データを追加（transit / pickup 時にテスト用ドライバー情報表示）
- `docs/pitch_deck.md`：ハイブリッドスケールモデル・シズナイロゴス競合分析・空港手荷物預かり比較込みの完全版（15スライド）
- `public/ceo-meeting-20260320.html`：CEO ミーティング議事録（プラットフォーム化宣言・GPS近接割引・富良野戦略）

---

## v0.5.0 — 2026-03-20

### ドライバーGPS追跡 + 日本国内旅行者対応

**新機能: ドライバーGPSリアルタイム追跡**
- `DriverView` — ドライバー専用画面（PIN認証: 4桁）
  - 追跡番号を入力して配送を管理
  - GPS追跡トグル（`navigator.geolocation.watchPosition` + 30秒ごと自動送信）
  - ステータス更新ボタン：向かっています → 近くにいます → 集荷中 → 配達完了
  - ステータス変更時に即時GPS位置送信
  - 複数配送を同時管理可能
- ヘッダーナビゲーションに🚐ボタン追加（ドライバー専用、目立ちにくい配置）
- `TrackingView` — ドライバー位置情報カードを追加
  - バックエンドポーリング（10秒ごと）
  - ドライバーとの距離表示（haversine計算）
  - ETA推定（平均30km/hベース）
  - 距離バー（集荷地点→配送先のプログレスバー）
  - LIVEパルスアニメーション
  - ドライバーステータスバッジ（向かっています / 近くにいます / 到着）
- バックエンド `PUT /bookings/{id}/driver-location` エンドポイント追加
  - BookingRecordに `driver_lat`, `driver_lng`, `driver_status`, `driver_updated_at` フィールド追加
  - ドライバーステータスに応じてBookingのstatusも自動更新
  - `GET /bookings/{id}` レスポンスにdriver情報を含める
- Next.js `/api/booking` にPUTプロキシハンドラ追加

**新機能: 日本国内旅行者対応**
- ヘッダーに「国内旅行 OK」バッジを追加（バイオレット配色）
- 国内旅行者向けの差別化ポイント：
  - JPYC（1 JPYC=¥1）で為替リスクなし、両替不要
  - 日本語UI完全対応
  - 千歳・札幌・小樽の国内観光スポット対応
  - 国内法人（ラピダス等）はJPYC + 法人プランで経費処理しやすい

---

## v0.4.0 — 2026-03-20

### JPYC決済対応 + 決済UI刷新

**新機能**
- **JPYC（日本円コイン）on Polygon** 決済に対応
  - 1 JPYC = ¥1 固定レート。為替リスクなし、0% 手数料
  - 法的分類：前払式支払手段（資金決済法）。暗号資産非該当
  - 支払い金額がそのままJPYC数量で表示（換算不要）
  - Polygonネットワーク対応QRコード・ウォレットアドレス表示
- 支払い方法を3択に拡張：クレジットカード / JPYC / USDC
  - JPYCタブはバイオレット配色で差別化
- 料金サマリーにJPYC・USDC両方の等価額を同時表示
- AIチャットのシステムプロンプト更新（JPYC・最新料金・スポット情報を反映）

**事業計画書更新（v1.2）**
- 第11章「JPYC × USDC 決済戦略 — メディア・PR角度」を追加
  - PRプレイブック（JPYC Inc.提携・メディアリリース先・ストーリーライン）
  - 「競合が真似できない理由」分析
  - 観光庁・北海道観光振興機構への展開シナリオ

---

## v0.3.0 — 2026-03-20

### 待ち合わせスポット可視化

**新機能**
- 集荷スポットカード（横スクロール）をゾーン別に表示
  - 千歳: 新千歳国際線到着ロビー(1F)・手荷物受取(1F)・国内線無印良品前(2F)・レラ・ホテルフロント前
  - 札幌: ステラプレイス・大通公園観光案内所・狸小路・ホテルフロント前
  - 小樽: 小樽運河観光案内所・堺町通りメルヘン交差点・ホテルフロント前
- タップで集荷場所テキストに自動入力。フリーテキスト上書きも可
- ホテル受け取りスポット選択（フロントカウンター前 / ベルデスク / 正面玄関 / 客室前）
- 空港配送スポット選択（国際線チェックイン前 / 無印良品前 / スタバ前 / ANA・JALカウンター前 / 到着ロビー）
- フロアバッジ（1F / 2F / B1）をスポットカードに表示

---

## v0.2.0 — 2026-03-20

### AI相乗りマッチングエンジン（バックエンド）

**新機能**
- `POST /bookings` エンドポイント — 予約作成 + 即時AIマッチング実行
- `GET /bookings/{id}` エンドポイント — 予約情報・マッチ状態取得
- `backend/matching.py` — 相乗りマッチングエンジン
  - 同スロット × 同ゾーン × 相乗りON × 未マッチの予約を自動グルーピング（最大3件）
  - グループ成立時に Claude haiku-4-5 へ最適集荷順とETA（分）を問い合わせ
  - 失敗時はデフォルト順にフォールバック（サービス継続性を保証）
- `BookingRecord` テーブル（SQLite/PostgreSQL）に予約データ永続化
- Next.js `/api/booking` プロキシルート（CORS・認証ハンドリング）
- フロント: 支払い確定時にAPIを呼び出し、実際のトラッキングIDを取得
- 確定画面にマッチ結果表示（マッチ人数・推定所要時間・Claudeの最適化理由）
- 送信中スピナー表示 / APIエラー時はクライアント側トラッキングIDにフォールバック

---

## v0.1.0 — 2026-03-20

### MVP フロントエンド + 料金・UX機能群

**新機能: 料金体系**
- Hokkaido MVP 料金テーブル（Solo / Pair / Family × chitose / sapporo / otaru）
- 急行保証料 +¥1,000（スロット選択時のみ加算）
- 相乗り割引 −15%（小計全体から計算）
- GPS空港割引 −¥500（新千歳空港15km圏内を自動検出）
- 料金内訳をStepPaymentで全項目表示（基本料・急行料・追加荷物・各割引・合計）
- 節約額をグリーンで強調表示

**新機能: 急行便スロット**
- FLIGHT_SLOTS — 国際線フライト時刻に対応した4便
  - 便①〜④（仁川・香港・台北便に連動）
- フライト番号入力 → 推奨スロット自動提案（`suggestSlot`）
- 推奨スロットに「おすすめ」バッジ表示

**新機能: 相乗りマッチングUI**
- 相乗りトグルボタン（緑色アクティブ）
- マッチング中アニメーション（パルス + AIバッジ）
- 「2名が同方向に予約中」表示

**新機能: ホテル検索**
- ゾーン別ホテルリスト（千歳11件・札幌12件・小樽7件）
- 全ゾーン横断検索対応
- フリーテキスト入力フォールバック

**新機能: キャンセルポリシー（確定画面）**
- 予約から10分以内: 全額返金
- 10分経過後: 90%返金（10%キャンセル料）
- 集荷後: 返金不可

**新機能: 法人向けページ（BusinessView）**
- ターゲット企業12社（ASML・Applied Materials・TEL・KLA・Advantest等）
- 法人プラン3種（スターター¥50,000 / ビジネス¥120,000 / エンタープライズ要相談）
- 問い合わせフォーム（会社名・担当者・月間配送件数）

**新機能: AIチャットサポート（ChatWidget）**
- Claude haiku-4-5 によるリアルタイムAIサポート
- `/api/chat` Next.js APIルート経由
- 4言語対応（EN/JA/ZH/KO）
- フローティングボタン（画面右下固定）

**エリア**
- 配送ゾーン: chitose（千歳・苫小牧・北広島）/ sapporo（札幌・東札幌）/ otaru（小樽）
- 富良野・ニセコは Phase 2（ドライバー増員後）

**多言語**
- 英語 / 日本語 / 繁體中文 / 한국어 の4言語完全対応

---

## インフラ

| レイヤー | 技術 | 環境 |
|--------|------|------|
| フロントエンド | Next.js 16 (App Router) | Vercel |
| バックエンド | FastAPI + SQLAlchemy | Railway |
| DB | PostgreSQL | Railway |
| AI（マッチング・チャット） | Claude haiku-4-5-20251001 | Anthropic API |
| 決済 | Stripe（テストモード）/ JPYC on Polygon / USDC on Solana | 本番稼働中 |
| メール | Resend | 本番稼働中（要ドメイン認証） |

---

## 既知の制限・今後の予定

| 項目 | 状態 |
|------|------|
| Stripe 本番キー設定 | ⬜ 未対応（テストモード） |
| Resend 独自ドメイン認証 | ⬜ 未対応（要 kairox.jp DNS 設定） |
| JPYC 本番ウォレットアドレス | ⬜ 未設定（プレースホルダー） |
| 成田・羽田エリア | ⬜ Phase 2（近日公開バッジ表示中） |
| 相乗りマッチング Push 通知 | ⬜ Phase 1 |
| 法人問い合わせフォーム バックエンド接続 | ⬜ Phase 1 |
| KKday / Klook 掲載 | ⬜ 申請準備中 |
| `GET /drivers/active` エンドポイント | ⬜ Phase 1（GPS テロップの実ドライバー連動） |
